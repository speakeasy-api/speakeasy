```typescript showLineNumbers copy filename="Status Get Error usage example"
import { SDK } from "openapi";
import { StatusGetErrorResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.errors.statusGetError({
  statusCode: 425451,
}).then((res: StatusGetErrorResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
