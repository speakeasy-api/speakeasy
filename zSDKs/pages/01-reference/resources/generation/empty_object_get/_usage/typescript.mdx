```typescript showLineNumbers copy filename="Empty Object Get usage example"
import { SDK } from "openapi";
import { EmptyObjectGetResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.generation.emptyObjectGet({
  emptyObject: {},
}).then((res: EmptyObjectGetResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
