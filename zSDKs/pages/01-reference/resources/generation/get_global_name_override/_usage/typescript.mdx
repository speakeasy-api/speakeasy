```typescript showLineNumbers copy filename="Get Global Name Override usage example"
import { SDK } from "openapi";
import { GetGlobalNameOverrideResponse1 } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.generation.globalNameOverridden().then((res: GetGlobalNameOverrideResponse1) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
