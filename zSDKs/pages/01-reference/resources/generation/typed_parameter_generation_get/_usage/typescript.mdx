```typescript showLineNumbers copy filename="Typed Parameter Generation Get usage example"
import { SDK } from "openapi";
import { TypedParameterGenerationGetResponse } from "openapi/dist/sdk/models/operations";
import { RFCDate } from "openapi/dist/sdk/types";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.generation.typedParameterGenerationGet({
  bigint: 974259,
  date: new RFCDate("2022-01-16"),
  decimal: 8623.1,
  obj: {
    bool: false,
    num: 1481.41,
    str: "whoosh SSD auxiliary",
  },
}).then((res: TypedParameterGenerationGetResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
