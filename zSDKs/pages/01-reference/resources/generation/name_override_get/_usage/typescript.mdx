```typescript showLineNumbers copy filename="Name Override Get usage example"
import { SDK } from "openapi";
import { NameOverrideGetEnumNameOverride, NameOverrideGetResponse1 } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.generation.nameOverride({
  testEnumQueryParam: NameOverrideGetEnumNameOverride.Value3,
  testQueryParam: "example",
}).then((res: NameOverrideGetResponse1) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
