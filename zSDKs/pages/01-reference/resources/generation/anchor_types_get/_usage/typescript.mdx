```typescript showLineNumbers copy filename="Anchor Types Get usage example"
import { SDK } from "openapi";
import { AnchorTypesGetResponse1 } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.generation.anchorTypesGet().then((res: AnchorTypesGetResponse1) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
