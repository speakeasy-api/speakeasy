```typescript showLineNumbers copy filename="Get Resource usage example"
import { SDK } from "openapi";
import { GetResourceResponse1 } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.resource.getResource({
  resourceId: "Gasoline",
}).then((res: GetResourceResponse1) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
