```typescript showLineNumbers copy filename="Update Resource usage example"
import { SDK } from "openapi";
import { UpdateResourceResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.resource.updateResource({
  resourceId: "Future multimedia laborum",
}).then((res: UpdateResourceResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
