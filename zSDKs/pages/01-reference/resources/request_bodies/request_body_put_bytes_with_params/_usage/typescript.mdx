```typescript showLineNumbers copy filename="Request Body Put Bytes With Params usage example"
import { SDK } from "openapi";
import { RequestBodyPutBytesWithParamsResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.requestBodies.requestBodyPutBytesWithParams({
  requestBody: "]p\/#!ux#:" as bytes <<<>>>,
  queryStringParam: "Outdoors",
}).then((res: RequestBodyPutBytesWithParamsResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
