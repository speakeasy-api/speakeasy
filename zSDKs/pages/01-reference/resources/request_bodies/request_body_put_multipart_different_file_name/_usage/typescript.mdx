```typescript showLineNumbers copy filename="Request Body Put Multipart Different File Name usage example"
import { SDK } from "openapi";
import { RequestBodyPutMultipartDifferentFileNameResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.requestBodies.requestBodyPutMultipartDifferentFileName({
  differentFileName: {
    content: "hisQ{'VLKI" as bytes <<<>>>,
    differentFileName: "Salad Djibouti",
  },
}).then((res: RequestBodyPutMultipartDifferentFileNameResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
