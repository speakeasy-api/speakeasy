```typescript showLineNumbers copy filename="Nullable Object Post usage example"
import { SDK } from "openapi";
import { NullableObjectPostResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.requestBodies.nullableObjectPost({
  optional: "Extended",
  required: 55107,
}).then((res: NullableObjectPostResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
