```typescript showLineNumbers copy filename="Request Body Write Only usage example"
import { SDK } from "openapi";
import { RequestBodyWriteOnlyResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.requestBodies.requestBodyWriteOnly({
  bool: false,
  num: 4965.48,
  string: "partnerships Tools black",
}).then((res: RequestBodyWriteOnlyResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
