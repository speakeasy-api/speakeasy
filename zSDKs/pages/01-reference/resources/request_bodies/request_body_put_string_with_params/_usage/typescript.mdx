```typescript showLineNumbers copy filename="Request Body Put String With Params usage example"
import { SDK } from "openapi";
import { RequestBodyPutStringWithParamsResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.requestBodies.requestBodyPutStringWithParams({
  requestBody: "Towels South",
  queryStringParam: "Personal",
}).then((res: RequestBodyPutStringWithParamsResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
