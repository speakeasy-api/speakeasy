```typescript showLineNumbers copy filename="Telemetry Speakeasy User Agent Get usage example"
import { SDK } from "openapi";
import { TelemetrySpeakeasyUserAgentGetResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.telemetry.telemetrySpeakeasyUserAgentGet({
  userAgent: "backing daily sternly",
}).then((res: TelemetrySpeakeasyUserAgentGetResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
