```typescript showLineNumbers copy filename="Primitive Type One Of Post usage example"
import { SDK } from "openapi";
import { PrimitiveTypeOneOfPostResponse1 } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.unions.primitiveTypeOneOfPost(false).then((res: PrimitiveTypeOneOfPostResponse1) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
