```typescript showLineNumbers copy filename="Response Body Additional Properties Date Post usage example"
import { SDK } from "openapi";
import { ResponseBodyAdditionalPropertiesDatePostResponse1 } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.responseBodies.responseBodyAdditionalPropertiesDatePost({
  "aliquid": new RFCDate("2021-11-26"),
}).then((res: ResponseBodyAdditionalPropertiesDatePostResponse1) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
