```typescript showLineNumbers copy filename="Response Body Empty With Headers usage example"
import { SDK } from "openapi";
import { ResponseBodyEmptyWithHeadersResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.responseBodies.responseBodyEmptyWithHeaders({
  xNumberHeader: 3429.21,
  xStringHeader: "woman Product synthesize",
}).then((res: ResponseBodyEmptyWithHeadersResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
