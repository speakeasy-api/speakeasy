```go showLineNumbers copy filename="Response Body Zero Value Complex Type Ptrs Post usage example"
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/shared"
	"math/big"
	"openapi/pkg/types"
)

func main() {
    s := openapi.New(
        openapi.WithSecurity(shared.Security{
            APIKeyAuth: openapi.String("Token YOUR_API_KEY"),
        }),
        openapi.WithGlobalPathParam(100),
        openapi.WithGlobalQueryParam("some example global query param"),
    )

    ctx := context.Background()
    res, err := s.ResponseBodies.ResponseBodyZeroValueComplexTypePtrsPost(ctx, shared.ObjWithZeroValueComplexTypePtrs{
        Bigint: big.NewInt(790463),
        BigintStr: big.NewInt(591065),
        Date: types.MustDateFromString("2020-01-01"),
        DateTime: types.MustTimeFromString("2020-01-01T00:00:00Z"),
        Decimal: types.MustNewDecimalFromString("5149.93"),
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.ResponseBodyZeroValueComplexTypePtrsPost200ApplicationJSONObject != nil {
        // handle response
    }
}
```
