```typescript showLineNumbers copy filename="Group Second Get usage example"
import { SDK } from "openapi";
import { GroupSecondGetResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.second.get().then((res: GroupSecondGetResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
