```typescript showLineNumbers copy filename="Oauth2 Auth New usage example"
import { SDK } from "openapi";
import { Oauth2AuthNewResponse, Oauth2AuthNewSecurity } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});
const operationSecurity: Oauth2AuthNewSecurity = {
  oauth2: "Bearer YOUR_OAUTH2_TOKEN",
};

sdk.authNew.oauth2AuthNew({
  basicAuth: {
    password: "7yaSsYhfUF54625",
    username: "Tyreek.Marks49",
  },
  headerAuth: [
    {
      expectedValue: "Handcrafted Bronze Austria",
      headerName: "virtual scarcely",
    },
  ],
}, operationSecurity).then((res: Oauth2AuthNewResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
