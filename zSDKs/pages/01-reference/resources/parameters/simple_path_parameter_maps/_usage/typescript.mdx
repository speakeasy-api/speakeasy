```typescript showLineNumbers copy filename="Simple Path Parameter Maps usage example"
import { SDK } from "openapi";
import { SimplePathParameterMapsResponse } from "openapi/dist/sdk/models/operations";

const sdk = new SDK({
  security: {
    apiKeyAuth: "Token YOUR_API_KEY",
  },
  globalPathParam: 100,
  globalQueryParam: "some example global query param",
});

sdk.parameters.simplePathParameterMaps({
  mapParam: {
    "alias": "capacitor",
  },
  mapParamExploded: {
    "ex": 958308,
  },
}).then((res: SimplePathParameterMapsResponse) => {
  if (res.statusCode == 200) {
    // handle response
  }
});
```
